import React, {  } from 'react';

export interface Props {
}

const RoomEmpty = () => {
    return (
        <div className="room">
            (empty room)
        </div>
    );
}

export default RoomEmpty;
